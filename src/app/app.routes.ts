// app.routes.ts
import { Routes } from '@angular/router';

// Import the main component or placeholder for the feature routes
// export const routes: Routes = [
//   { path: '', redirectTo: '', pathMatch: 'full' }, // If the user accesses the root ("/"), redirect to "/todos"
//   {
//     path: 'todos',
//     loadChildren: () => import('./todos/todos.routes').then((m) => m.routes),
//   }, // Lazy load "todos" feature
// ];
